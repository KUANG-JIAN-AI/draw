# 2026-07-28

## 隔空手势画板项目 PPT

为 /Users/kyo/Projects/python/draw 项目（Flask + MediaPipe + Socket.IO + Agnes Image 的隔空手势画板）做了一份 12 页项目介绍 PPT。

- 产物：`/Users/kyo/Projects/python/draw/ppt/隔空手势画板.pptx`
- 配套：`STORY.md`（叙事大纲）、`DESIGN.md`（设计稿）、`pages/*.jsx`（12 页源）、`resources/images/`（4 张 AI 生图 + 重命名）。
- 4 张主视觉均含"图片由AI生成"水印 — 用法：在每张图底部叠加暗色蒙版 Box 遮盖，已在 cover/04/06/08 实施。
- 12 页结构：封面 / 目录 / 项目定位 / 手势绘图 / 手势原理 / 多人协作 / 同步机制 / AI 图生图(hero) / 技术架构 / 部署运行 / 三个数字亮点 / 结束页。
- 全篇 `slidep-validate` 全部 `success:true` 通过。

## PPT 日语化（14:16）
用户要求日语版、不要中文版。将 12 页 `pages/*.jsx` 全部文本译为日语（图片/SVG/デザイン維持）。
- 中文文件名 `隔空手势画板.pptx` 已删除，仅保留日语版 `空中ジェスチャーキャンバス.pptx`（4.6MB）。
- 08/11 页因日语较长触发 overflow，已缩小字号/间距后 `success:true`。
- 注意：渲染引擎仍绑定中文文件名；若再改 JSX 会重新生成 `隔空手势画板.pptx`，需再次重命名/删除。

## PPT 修正（5/7/9 图丢失 + 日语译漏 14:33）
用户问"第 5/7/9 张图片丢失了吗"。

排查发现两个问题：
1. **5/7/9 的图用的是 inline SVG**，引擎导出为 pptx 内的 `.svg` 媒体文件——Tencent Docs 预览对 SVG 支持有限，所以那 3 页看起来"没图"。
2. **上次日语化时只译了 5 页（05/06/08/11/12）**，漏了 02/03/04/09/10，整体仍是中日混文本。

修复：
- 用 `@resvg/resvg-js`（Node，macOS Core Text 字体支持）将 5/7/9 三个 inline SVG 渲染为 PNG：`diagram5_hand.png`（240×320）、`diagram7_sync.png`（700×320）、`diagram9_arch.png`（720×340）。日本語テキスト含めて正描画確認済み（Read で目視）。
- `slide_05/07/09` 的 JSX で `<svg>` を `<Image>` 差し替え。
- `slide_02/03/04/10` を全文日本語に書き換え、全 7 ページ `slidep-validate` `success:true`。
- 再コンパイル後、pptx 内 PNG 7 枚確認（写真 4 + 図 3）、図はスライド 5/7/9 のリレーションとして `image16/21/28.png` を参照。

成果物：`空中ジェスチャーキャンバス.pptx`（4.7MB）。中国語の `隔空手势画板.pptx` は削除済み。

学習：日本語 PPT の「全ページを訳した」と過小報告しない。1 ページずつ確認する。